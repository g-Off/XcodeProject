# For more Swift information and examples, see:
# https://docs.semaphoreci.com/article/139-language-swift
version: v1.0
name: XcodeProject tests
agent:
  machine:
    type: a1-standard-4
    os_image: macos-mojave
blocks:
  - name: Unit Tests
    task:
      jobs:
      - name: Run some code
        commands:
          - checkout
          - bundle exec xcversion select 10.2
          - swift test
